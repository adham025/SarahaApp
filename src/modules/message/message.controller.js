import { Router } from "express";
const router = Router();
import * as messageController from "./service/message.service.js";

// router.get("/profile", auth(), messageController);

export default router;
